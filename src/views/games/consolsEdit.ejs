<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs', {title: "Editando " + consol.name }) %>
</head>

<body>
    <%- include("../partials/header.ejs") %>

        <section class="centrar">
            <h2 class="headlines">Editando consola: -<%= consol.name %>-
            </h2>

            <form action="/consols/<%= consol.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                <div class="margin-bottom-15">
                    <label for="name">Nombre de la consola</label>
                    <input class="box" type="text" name="name" id="name" value="<%= consol.name %>" />
                </div>

                <div class="margin-bottom-15">
                    <label for="image">Cargar imagen</label>
                    <div>
                        <input class="margin-bottom-20" type="file" name="image" accept=".jpg"
                            value="<%= consol.image %>" />
                    </div>
                </div>

                <div class="margin-bottom-15">
                    <label for="logo">Cargar logo</label>
                    <div>
                        <input class="margin-bottom-20" type="file" name="logo" accept=".jpg"
                            value="<%= consol.logo %>" />
                    </div>
                </div>

                <div class="buttons-send-erase" style="display: block; text-align:center ">
                    <button class="buttons" type="submit">Confirmar</button>

                </div>
            </form>

            <div class="buttons-send-erase" style="display: block; text-align:center ">
                <form action="/consols/<%= consol.id %>?_method=DELETE" method="POST">
                    <button class="buttons">Eliminar consola</button>
                </form>
            </div>

        </section>

        <%- include("../partials/footer.ejs") %>
</body>

</html>